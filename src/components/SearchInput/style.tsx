import styled from 'styled-components';

export const Input = styled.input`
  background-color: #f2f2f2;
  border-radius: 4px;
  padding: 4px;
  color: black;
  border: none;
  &:focus {
    outline: none;
  }
`;
